import HeroTitle from "../../components/HeroTitle";
import { HeadingBar, NavBar } from "../../shared";

const Home = () => {
  return (
    <div>
      <HeadingBar />
      <NavBar />
      <HeroTitle />
    </div>
  );
};

export default Home;
